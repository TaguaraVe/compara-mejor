import { mySubtitle } from '@/app/layout';
import React from 'react';

const Beneficts = () => {
  return (
    <section className="">
      <h1
        className={`${mySubtitle.className} pt-12 text-center text-4xl text-myBlue`}
      >
        Beneficios
      </h1>
    </section>
  );
};

export default Beneficts;
