import { mySubtitle } from '@/app/layout';
import React from 'react';

const Beneficts = () => {
  return (
    <section>
      <h1
        className={`${mySubtitle.className} pt-12 text-center text-4xl text-myGreen `}
      >
        Acerca de nosotros
      </h1>
    </section>
  );
};

export default Beneficts;
